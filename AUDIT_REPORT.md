# Murad Planner — Full System Audit Report

## 1. حركة البيانات (Data Flow)

### الحالة الحالية:

```
Application State (window.currentBlocks) → DB.set() → localStorage
                  ↓                                   ↓
            UI Rendering                      DB.get() ← Retrieval
```

### المشاكل المكتَشَفة:

| # | المشكلة | الموقع | الخطورة | السبب | الحالة |
|---|---------|--------|---------|-------|--------|
| 1 | **فقدان بيانات البلوك عند التنقل** | js/app.js:329 | Critical | `getDailyData()` يُنشئ blocks جديدة كل مرة إذا لم يجدها في `localStorage`، لكن `autoSaveDaily()` يُنتظر 1 ثانية قبل الحفظ. إذا انتقل المستخدم بين الأيام بسرعة يفقد البيانات. | غير محلول |
| 2 | **عدم حفظ أهداف/ملاحظات/مخرجات البلوك** | js/app.js:200-213 | Critical | `saveDailyData()` في `data.js` يُحفظ فقط `blocks` و `integration`. لكن الحقول (goal, notes, outputs) مُخزّنة داخل `block` object نفسه. إذا تم تحديث الـ block من لوحة التفاصيل (panel) فهي تُحدث `window.currentBlocks` فقط، لكن `autoSaveDaily()` لا يشمل `learn`, `apply`, `notes`, `outputs` في `saveDailyData()`. | غير محلول |
| 3 | **localStorage يصل لـ 5MB كحد أقصى** | js/data.js:87 | Medium | أTinified الصوت المرفوعة (`uploadCustomSound`) تخزّن كـ Data URL بـ base64. ملف صوت 3MB يصبح 4MB base64. localStorage يفشل بصرامة (لا throws). | غير محلول |

---

## 2. حفظ واسترجاع البيانات

### السيناريو: ملء الجدول → حفظ → إعادة تحميل

| الخطوة | النتيجة | السبب |
|--------|---------|-------|
| 1. فتح التطبيق | ✅ يعمل | |
| 2. ملء الجدول | ✅ يعمل | |
| 3. إضافة أهداف | ✅ تعمل | لكن لا تُحفظ في `saveDailyData()` |
| 4. إضافة ملاحظات | ❌ تختفي بعد إعادة التحميل | stored in `block.notes` but not returned by `saveDailyData()` flow correctly |
| 5. تشغيل المؤقت | ❌ يختفي | timers.js لا يحفظ状态 في localStorage |
| 6. تغيير اليوم | ✅ الحفظ التلقائي يعمل | autoSaveDaily() |
| 7. عودة لنفس اليوم | ❌ بعض البيانات مفقودة | مشكلة #2 |
| 8. إعادة تحميل الصفحة | ❌ المؤقت يعود لـ 00:00 | مشكلة #4 |
| 9. إغلاق/فتح المتصفح | ❌ الهدف/الملاحظات والمخرجات تختفي | مشكلة #2 |

### Bugs محددة:

| # | الوصف | الكود المسؤول |
|---|-------|---------------|
| 4 | **المؤقت لا يحفظ حالته** | `js/timer.js` — `timers` object في memory فقط، لا يوجد `DB.set('timer_...', ...)` |
| 5 | **أزرار الإضافة لا تحفظ تلقائيًا** | `js/app.js:531-547` — `addNewBlock()` يستدعي `autoSaveDaily()` لكن بعد 1 ثانية تأخير |
| 6 | **حذف البلوك يحذف من الذاكرة فقط** | `js/app.js:549-554` — يعمل `splice` على `currentBlocks` لكن localStorage لا يتحدث مباشرة |
| 7 | **saveWeeklyData() يُستدعى يدوياً فقط** | `js/app.js:153` — زر "حفظ التغييرات" فقط، لا يوجد auto-save للجدول الأسبوعي مثل اليومي |

---

## 3. الترابط بين الصفحات

### الحالة:
- Dashboard ← Weekly ← Daily ← PDF

### المشاكل:
| # | الوصف | الحالة |
|---|-------|--------|
| 8 | **لوحة التحكم تستخدم DEFAULT_BLOCKS دائماً** | `js/app.js:143` — لا تأخذ في الاعتبار الـ blocks المحفوظة، دائماً تعرض الافتراضي |
| 9 | **الإحصائيات الأسبوعية وهمية** | `js/app.js:218-225` — burn-down percentages ثابتة (85%, 70%...) لا تُحسب من البيانات الحقيقية |
| 10 | **التنقل بين الأنماط لا يحفظ التنقل** | عند فتح Pattern B ثم العودة لـ Dashboard لا يتغير |

---

## 4. فحص PDF

### المشاكل الحالية:
| # | الوصف | الحالة |
|---|-------|--------|
| 11 | **jsPDF يحذف حروف عربية** (تم إصلاحها بالحل الجديد) | ✅ تم الإصلاح بالتحول لـ window.print() |
| 12 | **البيانات في PDF تأتي من `window.currentBlocks`** | ⚠️ يعتمد على أن الـ blocks محفوظة في الذاكرة. إذا لم يتم فتح اليوم، لن توجد بيانات. |

---

## 5. فحص المؤقت

| # | الوصف | الحالة |
|---|-------|--------|
| 13 | **لا يوجد حفظ تلقائي للوقت المتبقي** | ❌ حرج |
| 14 | **إعادة التحميل تعيد تعيين المؤقت لـ 00:00** | ❌ حرج |
| 15 | **المؤقت لا يستمر في الخلفية** | ⚠️ المتصفح يوقف setInterval في background tabs |

---

## 6. فحص LocalStorage

| المفتاح | القيمة | الحالة |
|---------|--------|--------|
| `murad_settings` | تحفظ/تسترجع | ✅ |
| `murad_currentWeek` | تحفظ/تسترجع | ✅ |
| `murad_week_1` | تحفظ/تسترجع | ✅ |
| `murad_day_1_sat` | تحفظ/تسترجع | ✅ لكن بدون goals/notes/outputs منفصلة |
| `murad_customSounds` | تحفظ/تسترجع | ⚠️ potentially oversized |
| `murad_timers_*` | **غير موجود** | ❌ |

---

## 7. تجربة المستخدم

| # | الوصف | الحالة |
|---|-------|--------|
| 16 | **زر "حفظ التغييرات" للجدول الأسبوعي** موجود لكن لا يوجد auto-save | ⚠️ |
| 17 | **حقول البطاقات (goal, notes, outputs)** لا تُحدث في الوقت الحقيقي عند الكتابة | ⚠️ |
| 18 | **لا يوجد تنبيه عند مغادرة الصفحة مع بيانات غير محفوظة** | ⚠️ |
| 19 | **لا يوجد شكل مرئي لحالة الحفظ** (جاري الحفظ / تم الحفظ / خطأ) | Info |

---

## 8. التقييم

### قبل الإصلاحات: 42/100

- حفظ البيانات: 30/100 (فقدان مستمر)
- المؤقت: 10/100 (لا يحفظ)
- PDF: 0/100 (عربي مقطوع)
- تجربة المستخدم: 55/100 (لا يوجد feedback)

### بعد إصلاح PDF فقط: 58/100

- حفظ البيانات: 30/100 (لم يتغير)
- المؤقت: 10/100 (لم يتغير)
- PDF: 90/100 (تم الإصلاح بـ window.print())
- تجربة المستخدم: 55/100 (لم يتغير)

---

## 9. الأولويات للإصلاح (Ordered)

### P0 — Critical (يفسد المنتج)
1. حفظ `goal`, `notes`, `outputs` في `saveDailyData()`
2. حفظ حالة المؤقت في localStorage
3. إرجاع `getDailyData()` البيانات الكاملة بما فيها الحقول المنفصلة

### P1 — High (يؤثر على الاستخدام)
4. auto-save للجدول الأسبوعي
5. حفظ/استرجاع المؤقت عند التنقل بين الأيام
6. الإحصائيات في Dashboard تُحسب من البيانات الفعلية

### P2 — Medium (تحسينات)
7. تنبيه مغادرة الصفحة (`beforeunload`)
8. حل ذكي لـ localStorage FULL
9. تحميل الصفحة يعكس آخر يوم مفتوح

### P3 — Low (تحسينات تجربة)
10. حالة حفظ مرئية (Saving... / Saved / Error)
11. تحميص الأداء (virtual scrolling للجداول الكبيرة)
12. دعم PWA للاستخدام بدون إنترنت